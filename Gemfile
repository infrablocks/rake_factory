# frozen_string_literal: true

source 'https://rubygems.org'

# Specify your gem's dependencies in rake_tasklib.gemspec
gemspec

# Hold these to their stable lines: a floor-only requirement lets bundler
# prefer a locally-cached prerelease that is not published on rubygems.org.
gem 'rake_git', '~> 0.2.0'
gem 'rake_git_crypt', '~> 0.4.0'
